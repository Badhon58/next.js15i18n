import SearchHandler from "@/components/SearchComponent/SearchHandler";
import React, { Suspense } from "react";

const page = () => {
  return (
    <Suspense>
      {/* <SearchHandler /> */}
    </Suspense>
  );
};

export default page;
